---
import type { Tables } from "../../supabase/types.ts";

interface Props {
  framework: Tables<"frameworks">;
}

const { framework } = Astro.props;
---

<tr class="border-b border-neutral-600">
  <td class="text-xl font-bold">{framework.mobile}</td>
  <td>
    <img
      src={`/.netlify/images?url=/images/${framework.name}`}
      class="w-32"
      alt={framework.mobile}
    />
  </td>
  <td>{framework.points} like{framework.points === 1 ? "" : "s"}</td>
  <td><a href={`/frameworks/${framework.id}`} class="underline">Learn more &rarr;</a></td>
</tr>
